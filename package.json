{
  "name": "iota-token-creator-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "E2E testnet script for IOTA Token Creator — creates all 3 coin types and outputs explorer URLs",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e:testnet": "tsx e2e/create-all-tokens.ts",
    "e2e:testnet:dotenv": "tsx -r dotenv/config e2e/create-all-tokens.ts"
  },
  "dependencies": {
    "@iota/iota-sdk": "^1.10.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.0.0"
}
