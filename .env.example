# IOTA Token Creator E2E Testnet Script — Environment Configuration
#
# Copy this file to .env and fill in your values.
# All values below are the defaults used when the env var is not set.

# API URL — must match docker-compose port mapping (8090:8080)
API_URL=http://localhost:8090

# IOTA Network RPC endpoint
IOTA_RPC_URL=https://api.testnet.iota.cafe

# IOTA Testnet Faucet endpoint
IOTA_FAUCET_URL=https://faucet.testnet.iota.cafe

# IOTA Explorer base URL (no trailing slash)
IOTA_EXPLORER_URL=https://explorer.iota.org/testnet

# Optional: Reuse a pre-funded keypair across runs.
# If set, must be the hex-encoded 32-byte private key (Ed25519).
# If not set, a new random keypair is generated each run.
# PRIVATE_KEY=0xabcdef...
